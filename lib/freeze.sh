# freeze script
# loop through each directory and grab the package names and push them to stdout
# can pass / parse options here as needed as well
freeze() {
	
	# loop through all currently installed go dependencies and output to stdout
	echo ""	

}

freeze_help() {

	echo "freeze: print out all currently installed packages to standard out"
}

